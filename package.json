{
  "name": "mre-oracle-edge",
  "version": "1.0.0",
  "description": "Oracle evaluator for MRE on Vercel Edge",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "typescript": "^5.3.0"
  }
}
